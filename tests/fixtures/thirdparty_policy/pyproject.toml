[project]
name = "thirdparty-policy"
version = "0.0.1"
description = "Fixture: an admission policy shipped by someone other than AdmitPerf"
requires-python = ">=3.11"

[project.entry-points."admitperf.policies"]
third_party_reject_all = "thirdparty_policy:RejectEverything"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
